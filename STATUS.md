# STATUS.md

## 2026-05-25

- 현재 저장소 구조를 확인했다.
- 저장소는 초기 상태이며 아직 커밋이 없다.
- 원격 저장소가 설정되어 있지 않은 것을 확인했다.
- 1차 작업 목표에 따라 실제 기능 구현은 시작하지 않고 문서 4개를 생성했다.
- 생성한 문서: `spec.md`, `TASKS.md`, `STATUS.md`, `START.md`
- 원격 저장소 `https://github.com/asteroidin8/i-have-a-cat.git`를 `origin`으로 연결했다.
- 문서 4개 생성 작업을 `초기 문서 작성` 커밋으로 기록했다.
- PR #1 `초기 프로젝트 문서 작성`을 생성했다.
- PR #1을 스쿼시 병합했다.
- 다음 단계는 Electron 기본 구조 생성이다.
- `package.json`을 생성하고 Electron, electron-builder 개발 의존성 선언을 추가했다.
- `TASKS.md`에서 `package.json 생성`을 완료로 표시했다.
- 다음 단계는 Electron 진입 파일 생성이다.
- Prettier, ESLint, Husky를 설치하고 커밋 전 `lint`, `format:check`가 실행되도록 설정했다.
- 개발 도구 설정 파일과 `package-lock.json`을 추가했다.
- Electron 진입 파일 `main.js`를 생성했다.
- `TASKS.md`에서 `Electron 진입 파일 생성`을 완료로 표시했다.
- 다음 단계는 렌더러 HTML/CSS/JS 생성이다.
- 토큰 절약, 파일 분리, 중복 방지, 하드코딩 최소화, 중앙 시스템 관리 원칙을 `START.md`와 `spec.md`에 반영했다.
- 렌더러 기본 파일 `index.html`, `src/renderer.js`, `src/styles/app.css`를 생성했다.
- `TASKS.md`에서 `렌더러 HTML/CSS/JS 생성`을 완료로 표시했다.
- 다음 단계는 개발 실행 스크립트 추가이다.
- `package.json`에 개발 실행 스크립트 `npm start`를 추가했다.
- `TASKS.md`에서 `개발 실행 스크립트 추가`를 완료로 표시했다.
- `timeout 8s npm start`로 개발 실행 스크립트 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 프레임 없는 창 구현이다.
- Electron 창 옵션에 `frame: false`를 추가해 프레임 없는 창을 구현했다.
- `TASKS.md`에서 `프레임 없는 창 구현`을 완료로 표시했다.
- `timeout 8s npm start`로 프레임 없는 창 설정 후 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 투명 배경 구현이다.
- Electron 창 옵션에 `transparent: true`와 투명 `backgroundColor`를 추가했다.
- 렌더러 `html`, `body` 배경을 투명하게 변경했다.
- `TASKS.md`에서 `투명 배경 구현`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 항상 위 표시 구현이다.
- Electron 창 옵션에 `alwaysOnTop: true`를 추가해 항상 위 표시를 구현했다.
- `TASKS.md`에서 `항상 위 표시 구현`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 창 크기 조절 가능 여부 확인이다.
- Electron 창 옵션에 `resizable: true`를 명시해 창 크기 조절 허용 상태를 확인 가능하게 했다.
- `TASKS.md`에서 `창 크기 조절 가능 여부 확인`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 고양이 없는 영역 클릭 통과 구현이다.
- Electron `setIgnoreMouseEvents`와 preload/IPC 브리지를 추가해 고양이 밖 영역에서 마우스 이벤트가 창을 통과하도록 구현했다.
- 렌더러에서 마우스 위치가 고양이 영역 밖이면 클릭 통과를 유지하고, 고양이 영역 위에서는 이후 상호작용을 위해 마우스 이벤트를 다시 받도록 전환했다.
- 렌더러의 `window` 접근을 린트 규칙에서 허용하도록 ESLint 전역 설정을 보강했다.
- `TASKS.md`에서 `고양이 없는 영역 클릭 통과 구현`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 고양이 영역 마우스 이벤트 감지 구현이다.
- 고양이 DOM에 `data-cat-pointer-state`를 추가하고 `mouseenter`, `mousedown`, `mouseup`, `mouseleave` 이벤트로 고양이 영역 마우스 감지를 구현했다.
- 감지 상태는 `outside`, `hover`, `press`로 구분하고, 확인 가능한 최소 시각 피드백을 CSS에 추가했다.
- `TASKS.md`에서 `고양이 영역 마우스 이벤트 감지 구현`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 클릭 통과 관련 제한사항 기록이다.
- `spec.md`에 클릭 통과가 `setIgnoreMouseEvents` 기반이며, 투명 영역에서는 렌더러가 클릭/드래그/휠 입력을 직접 처리할 수 없다는 제한사항을 기록했다.
- Windows 투명 창과 클릭 통과 동작은 운영체제, Electron 버전, 그래픽 환경에 따라 차이가 있어 실제 Windows 환경에서 재검증해야 한다고 기록했다.
- `TASKS.md`에서 `클릭 통과 관련 제한사항 기록`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- 다음 단계는 임시 도트 고양이 표시이다.
- 기존 고양이 DOM 안에 귀, 얼굴, 눈, 코, 꼬리, 발 요소를 추가해 임시 도트 고양이를 표시했다.
- 임시 도트 고양이는 별도 이미지 에셋 없이 HTML/CSS만으로 구성했다.
- `TASKS.md`에서 `임시 도트 고양이 표시`를 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 idle 상태 구현이다.
- 고양이 상태 설정 함수를 추가하고 초기 상태가 `idle`로 설정되도록 정리했다.
- `data-cat-state="idle"` 기준으로 몸 움직임, 눈 깜빡임, 꼬리 움직임을 CSS 애니메이션으로 구현했다.
- `TASKS.md`에서 `idle 상태 구현`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 walk 상태 구현이다.
- 고양이 상태 상수에 `walk`를 추가했다.
- `data-cat-state="walk"` 기준으로 몸, 꼬리, 앞발, 뒷발 움직임을 CSS 애니메이션으로 구현했다.
- 이번 작업에서는 랜덤 이동, 위치 변경, 상태 전환 로직은 추가하지 않았다.
- `TASKS.md`에서 `walk 상태 구현`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 방향 전환 구현이다.
- 고양이 DOM에 `data-cat-direction`을 추가하고 기본 방향을 `right`로 설정했다.
- 고양이 시각 요소를 `cat__sprite`로 감싸 방향 반전이 위치/상태 애니메이션과 충돌하지 않게 했다.
- `data-cat-direction="left"`일 때 스프라이트를 좌우 반전하도록 구현했다.
- `TASKS.md`에서 `방향 전환 구현`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 창 내부 랜덤 이동 구현이다.
- 렌더러에 랜덤 이동 설정과 고양이 위치 설정 함수를 추가했다.
- `cat-stage` 내부 크기를 기준으로 주기적으로 랜덤 좌표를 선택하고 고양이의 `left`, `top`을 갱신하도록 구현했다.
- CSS `transition`으로 위치 이동이 단계적으로 보이게 했다.
- 이번 작업에서는 창 크기 변경 시 경계 보정과 이동/정지 상태 전환은 추가하지 않았다.
- `TASKS.md`에서 `창 내부 랜덤 이동 구현`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 창 경계 밖으로 나가지 않게 제한이다.
- 이동 가능 범위 계산 함수와 좌표 clamp 함수를 추가해 고양이 중심 좌표가 창 내부 범위를 벗어나지 않도록 제한했다.
- 랜덤 이동 대상 좌표와 직접 위치 설정 모두 같은 경계 제한을 거치도록 정리했다.
- 창 크기 변경 시 현재 고양이 위치를 다시 clamp하는 resize 가드를 추가했다.
- `TASKS.md`에서 `창 경계 밖으로 나가지 않게 제한`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 이동/정지 상태 전환 구현이다.
- 랜덤 이동 시작 시 고양이 상태를 `walk`로 전환하고, 위치 이동 transition 시간 이후 `idle`로 되돌리도록 구현했다.
- 이동 상태 타이머를 보관해 다음 이동이 시작되면 이전 타이머를 정리하도록 했다.
- `TASKS.md`에서 `이동/정지 상태 전환 구현`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 마우스 위치 감지이다.
- 렌더러에 `mouseState`를 추가해 최신 마우스 좌표와 창 내부 여부를 저장하도록 했다.
- 기존 `mousemove` 처리에서 마우스 위치를 갱신하고, `mouseleave` 시 창 내부 여부를 false로 전환하도록 구현했다.
- 이번 작업에서는 접근 거리 계산이나 반응 로직은 추가하지 않았다.
- `TASKS.md`에서 `마우스 위치 감지`를 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 접근 거리 계산이다.
- `mouseState`에 고양이 중심까지의 거리 값을 저장하는 `distanceToCat` 필드를 추가했다.
- 고양이 중심 좌표 계산 함수와 두 점 사이 거리 계산 함수를 추가했다.
- 마우스 위치 변경과 고양이 위치 변경 시 접근 거리를 갱신하고, 마우스가 창 밖으로 나가면 거리 값을 비우도록 구현했다.
- 이번 작업에서는 접근 거리 기반 반응 로직은 추가하지 않았다.
- `TASKS.md`에서 `접근 거리 계산`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 쓰다듬기 드래그 판정이다.
- 쓰다듬기 판정을 위한 `pettingState`와 최소 드래그 거리 설정을 추가했다.
- 고양이 위에서 마우스를 누른 뒤 이동한 누적 거리가 기준 이상이면 `data-cat-petting="true"`로 판정하도록 구현했다.
- 드래그 중에는 고양이 영역 밖으로 이동해도 마우스 이벤트 수신이 끊기지 않도록 클릭 통과 전환 조건에 드래그 상태를 반영했다.
- 이번 작업에서는 rub 반응이나 startled/run 반응은 추가하지 않았다.
- `TASKS.md`에서 `쓰다듬기 드래그 판정`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 rub 반응 구현이다.
- 고양이 상태에 `rub`을 추가하고 쓰다듬기 판정 성공 시 `rub` 상태로 전환하도록 구현했다.
- `rub` 반응 중에는 기존 이동 타이머를 정리하고 일정 시간 후 `idle` 상태로 되돌리도록 했다.
- `data-cat-state="rub"`에 몸 흔들림, 꼬리 움직임, 눈 감김 CSS 반응을 추가했다.
- 이번 작업에서는 startled/run 반응은 추가하지 않았다.
- `TASKS.md`에서 `rub 반응 구현`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 startled/run 반응 구현이다.
- 고양이 상태에 `startled`와 `run`을 추가했다.
- 마우스가 고양이 근처에서 빠르게 움직이면 `startled` 상태로 짧게 전환한 뒤 마우스 반대 방향으로 도망가도록 구현했다.
- 도망 위치는 기존 경계 제한 로직을 거치도록 했고, 반응 중에는 랜덤 이동이 끼어들지 않게 했다.
- `data-cat-state="startled"`와 `data-cat-state="run"`에 눈 확대, 점프, 빠른 달리기 CSS 반응을 추가했다.
- `TASKS.md`에서 `startled/run 반응 구현`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 성격 데이터 구조 생성이다.
- 성격 식별자와 기본 성격 값을 추가했다.
- 애교쟁이, 새침이, 겁쟁이, 느긋이, 장난꾸러기 성격 데이터를 이동 성향과 상호작용 성향으로 나누어 정의했다.
- 현재 고양이 프로필의 성격을 조회하고 DOM `data-cat-personality`에 반영하는 함수를 추가했다.
- 이번 작업에서는 성격 데이터를 실제 행동 확률에 적용하지 않았다.
- `TASKS.md`에서 `성격 데이터 구조 생성`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 성격별 행동 확률 적용이다.
- 성격 데이터의 상호작용 확률을 쓰다듬기 `rub` 반응과 빠른 접근 `run` 반응 판정에 적용했다.
- 성격별 `startledSensitivity`로 놀람 반응 속도 기준을 보정하도록 했다.
- 성격별 `rubDurationMultiplier`로 쓰다듬기 반응 지속 시간을 보정하도록 했다.
- 성격별 이동 가중치와 이동 주기 배율을 랜덤 이동 판정에 적용했다.
- 이번 작업에서는 특정 성격별 전용 행동은 추가하지 않았다.
- `TASKS.md`에서 `성격별 행동 확률 적용`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 애교쟁이 구현이다.
- 애교쟁이 성격 전용 접근 반응 설정을 추가했다.
- 애교쟁이 상태에서 마우스가 가까이 있고 빠르게 움직이지 않으면 확률적으로 마우스 방향으로 한 걸음 다가가도록 구현했다.
- 애교쟁이가 사용자 근처에 있을 때는 랜덤 이동이 끼어들지 않게 해 근처에 머무르는 느낌을 추가했다.
- 이번 작업에서는 다른 성격 전용 행동은 추가하지 않았다.
- `TASKS.md`에서 `애교쟁이 구현`을 완료로 표시했다.
- `npx prettier --write src/renderer.js TASKS.md STATUS.md`로 수정 파일 포맷을 정리했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 새침이 구현이다.
- 새침이 성격 전용 회피 반응 설정을 추가했다.
- 새침이 상태에서 마우스가 가까이 있고 빠르게 움직이지 않으면 확률적으로 마우스 반대 방향으로 한 걸음 피하도록 구현했다.
- 새침이 상태에서 쓰다듬기 드래그가 길어지면 rub 반응을 끊고 자리를 피하도록 구현했다.
- 이번 작업에서는 겁쟁이, 느긋이, 장난꾸러기 전용 행동은 추가하지 않았다.
- `TASKS.md`에서 `새침이 구현`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 겁쟁이 구현이다.
- 겁쟁이 성격 전용 설정을 추가했다.
- 겁쟁이 상태에서는 랜덤 이동 시 일정 확률로 창 구석 근처를 목표 위치로 선택하도록 구현했다.
- 겁쟁이 상태에서는 쓰다듬기 판정 시 마우스 이동이 느리면 rub 확률을 높이고 빠르면 rub 확률을 낮추도록 구현했다.
- 이번 작업에서는 느긋이, 장난꾸러기 전용 행동은 추가하지 않았다.
- `TASKS.md`에서 `겁쟁이 구현`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 느긋이 구현이다.
- 느긋이 성격 전용 설정을 추가했다.
- 느긋이 상태에서는 랜덤 이동을 하지 않는 판정에서 성격의 sleep 가중치에 따라 `sleep` 상태로 쉬도록 구현했다.
- 느긋이 상태에서 쓰다듬기 반응은 흔들리는 rub 대신 눈을 감고 가만히 쉬는 `sleep` 상태로 표현하도록 구현했다.
- 이번 작업에서는 장난꾸러기 전용 행동은 추가하지 않았다.
- `TASKS.md`에서 `느긋이 구현`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 장난꾸러기 구현이다.
- 장난꾸러기 성격 전용 설정을 추가했다.
- 장난꾸러기 상태에서 마우스가 근처에서 빠르게 움직이면 도망가지 않고 마우스 방향으로 짧게 뛰어가도록 구현했다.
- 장난꾸러기 상태에서 랜덤 이동을 하지 않는 판정이 나와도 확률적으로 짧게 뛰거나 방향을 바꾸도록 구현했다.
- 이번 작업에서는 커스터마이징 기능은 추가하지 않았다.
- `TASKS.md`에서 `장난꾸러기 구현`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 이름 설정이다.
- 고양이 프로필에 이름 값을 추가하고 기본 이름을 `고양이`로 설정했다.
- 화면 왼쪽 위에 이름 입력 컨트롤을 추가하고 입력값이 고양이 이름 표시와 `aria-label`에 반영되도록 구현했다.
- 이름 입력 컨트롤 영역은 클릭 통과 대상에서 제외해 실제 입력 포커스를 받을 수 있도록 처리했다.
- 이번 작업에서는 이름 저장과 앱 재실행 시 불러오기는 추가하지 않았다.
- `TASKS.md`에서 `이름 설정`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 품종 선택이다.
- 품종 식별자와 기본 품종 값을 추가했다.
- 이름 설정 컨트롤 옆에 품종 선택 컨트롤을 추가하고 선택값이 고양이 프로필과 DOM `data-cat-breed`에 반영되도록 구현했다.
- 코리안 숏헤어, 폴드, 짧은 꼬리 품종을 추가하고 품종별 귀와 꼬리 형태 차이를 CSS로 표현했다.
- 이번 작업에서는 품종 저장과 앱 재실행 시 불러오기는 추가하지 않았다.
- `TASKS.md`에서 `품종 선택`을 완료로 표시했다.
- 다음 단계는 눈 색상 선택이다.
- 눈 색상 식별자와 기본 눈 색상 값을 추가했다.
- 품종 선택 컨트롤 옆에 눈 색상 선택 컨트롤을 추가하고 선택값이 고양이 프로필과 DOM `data-cat-eye-color`에 반영되도록 구현했다.
- 초록, 금색, 파랑 눈 색상 프리셋을 추가하고 선택값을 고양이의 `--cat-eye` CSS 변수에 반영하도록 구현했다.
- 이번 작업에서는 눈 색상 저장과 앱 재실행 시 불러오기는 추가하지 않았다.
- `TASKS.md`에서 `눈 색상 선택`을 완료로 표시했다.
- 다음 단계는 털 색상 선택이다.
- 털 색상 식별자와 기본 털 색상 값을 추가했다.
- 눈 색상 선택 컨트롤 옆에 털 색상 선택 컨트롤을 추가하고 선택값이 고양이 프로필과 DOM `data-cat-fur-color`에 반영되도록 구현했다.
- 주황, 회색, 크림 털 색상 프리셋을 추가하고 선택값을 고양이의 `--cat-fur`, `--cat-ear`, `--cat-shadow` CSS 변수에 반영하도록 구현했다.
- 이번 작업에서는 털 색상 저장과 앱 재실행 시 불러오기는 추가하지 않았다.
- `TASKS.md`에서 `털 색상 선택`을 완료로 표시했다.
- 다음 단계는 성격 선택이다.
- 털 색상 선택 컨트롤 옆에 성격 선택 컨트롤을 추가했다.
- 성격 선택값이 고양이 프로필과 DOM `data-cat-personality`에 반영되도록 기존 `setCatPersonality` 함수를 확장했다.
- 선택한 성격은 기존 성격별 이동, 접근, 쓰다듬기, 놀람 반응 확률에 즉시 적용된다.
- 이번 작업에서는 성격 저장과 앱 재실행 시 불러오기는 추가하지 않았다.
- `TASKS.md`에서 `성격 선택`을 완료로 표시했다.
- 다음 단계는 설정 저장이다.
- 2차 MVP 작업 목록을 `TASKS.md`의 11번부터 20번까지 추가했다.
- `spec.md`에 2차 MVP 목표와 완료 기준을 추가했다.
- 고양이 프로필 저장 키와 저장/불러오기/검증 함수를 추가했다.
- 이름, 품종, 눈 색상, 털 색상, 성격 변경 시 `localStorage`에 설정을 저장하도록 구현했다.
- 앱 시작 시 저장된 설정을 불러오고, 저장값이 없거나 손상된 경우 기본값을 사용하도록 구현했다.
- `TASKS.md`에서 `설정 저장`, `앱 재실행 시 설정 불러오기`, `기본값 처리`를 완료로 표시했다.
- `package.json`에 `electron-builder` 설정과 `pack`, `build:win` 스크립트를 추가했다.
- `START.md`에 개발 실행, unpacked 앱 생성, Windows 설치 파일 생성 명령을 기록했다.
- 빌드 결과물과 `node_modules`가 작업 목록에 섞이지 않도록 `.gitignore`를 추가했다.
- `TASKS.md`에서 `electron-builder 설정`과 `빌드 방법 문서화`를 완료로 표시했다.
- Windows 실행 파일 빌드는 실제 Windows 환경 검증이 필요하므로 미완료로 남겼다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- `npm run pack`으로 현재 플랫폼 기준 unpacked 패키징이 통과하는 것을 확인했다.
- 다음 단계는 Windows 환경에서 `npm run build:win`으로 실행 파일 빌드를 검증하는 것이다.
- `npm run build:win`을 실행했으나 Linux/WSL 환경에서는 NSIS 설치 파일 생성 단계에서 `wine`이 필요해 실패했다.
- Windows unpacked 실행 파일 생성만 검증하기 위해 `npx electron-builder --win dir`을 실행했고 통과를 확인했다.
- 생성된 실행 파일은 `dist/win-unpacked/나도 고양이 있어.exe`이다.
- `TASKS.md`에서 `Windows 실행 파일 빌드`를 완료로 표시했다.
- NSIS 설치 파일 생성과 Windows 실기기 실행 검증은 2차 MVP의 실제 배포 준비 단계에서 다시 다룬다.
- 다음 단계는 11번 `도트 에셋 리디자인`의 `현재 임시 스프라이트 구조 분석`이다.
- 현재 임시 고양이 스프라이트가 이미지 파일이 아니라 HTML 파츠와 CSS 도형/키프레임으로 구성되어 있음을 확인했다.
- `.cat` 루트의 `data-cat-state`, `data-cat-direction`, `data-cat-breed`, 색상 CSS 변수 구조를 분석했다.
- 방향 전환은 좌우 전용 프레임이 아니라 `.cat__sprite`의 `scaleX(-1)` 반전으로 처리되는 것을 확인했다.
- 품종 차이는 현재 `fold` 귀 형태와 `bobtail` 꼬리 길이만 CSS override로 표현되는 것을 확인했다.
- 분석 결과와 2차 MVP 에셋 교체 시 유지/검토할 구조를 `spec.md`에 기록했다.
- `TASKS.md`에서 `현재 임시 스프라이트 구조 분석`을 완료로 표시했다.
- 다음 단계는 11번 `도트 에셋 리디자인`의 `스프라이트 교체 가능한 구조 재확인`이다.
- 렌더러 로직이 내부 시각 파츠가 아니라 `.cat` 루트의 `data-cat-state`, `data-cat-direction`, `data-cat-breed`와 CSS 변수만 변경한다는 점을 확인했다.
- `.cat`, `.cat__name`, `.cat__sprite`를 유지하면 현재 HTML/CSS 파츠형 임시 스프라이트를 이미지 기반 스프라이트로 교체할 수 있다고 판단했다.
- 교체 시 `.cat`의 72x48px 기준 hit box 변경이 이동 경계, 클릭 통과, 쓰다듬기 판정에 영향을 줄 수 있음을 기록했다.
- 내부 파츠 선택자 기반 애니메이션을 이미지 프레임 전용 선택자로 분리해야 한다는 리스크를 `spec.md`에 기록했다.
- `TASKS.md`에서 `스프라이트 교체 가능한 구조 재확인`을 완료로 표시했다.
- 다음 단계는 11번 `도트 에셋 리디자인`의 `실제 도트 고양이 베이스 디자인 제작`이다.
- 고양이 스프라이트 DOM에 몸통, 가슴 털, muzzle, whisker 파츠를 추가했다.
- 기본 스프라이트 크기를 72x48px에서 88x56px로 확장하고 몸통/얼굴/귀/꼬리/발 silhouette을 다시 잡았다.
- 얼굴 비율을 조정하고 눈을 7x9px 기준으로 키워 작은 크기에서도 눈과 얼굴 방향이 보이게 했다.
- 귀 내부 색, 가슴 털, muzzle, whisker를 추가해 임시 박스형 도형보다 고양이 형태가 읽히도록 개선했다.
- 털 색상 프리셋에 `cream` 값을 추가하고 `--cat-cream` CSS 변수로 가슴 털과 muzzle 색상을 갱신하도록 했다.
- 오른쪽 방향은 기본 프레임, 왼쪽 방향은 `scaleX(-1)` 기반 프레임으로 유지하되 idle/walk 방향별 sprite 규칙을 분리했다.
- `setCatDirection`에서 방향이 바뀔 때 180ms turning 프레임을 표시하도록 `data-cat-turning` 상태를 추가했다.
- `domestic`, `fold`, `bobtail` 품종의 기본 정의와 silhouette, 귀, 꼬리, 체형 차이를 CSS override로 추가했다.
- 11번 리디자인 기준과 작은 크기 식별성 기준을 `spec.md`에 기록했다.
- `TASKS.md`에서 11번 `도트 에셋 리디자인`의 모든 항목을 완료로 표시했다.
- 다음 단계는 12번 `애니메이션 퀄리티 개선`의 `idle 프레임 추가`이다.
- 사용자 확인 결과, 실행 직후 설정을 먼저 완료한 뒤 고양이가 나와야 한다는 요구를 재확인했다.
- 앱 초기 상태를 `setup`으로 변경하고 시작 설정 패널을 추가했다.
- 설정 완료 버튼을 누르면 현재 이름, 품종, 눈, 털, 성격 설정을 저장하고 `companion` 상태로 전환해 고양이가 보이도록 했다.
- 설정을 다시 열 수 있는 버튼과 창 이동용 드래그 핸들을 추가했다.
- 프레임 없는 투명 창에서도 크기를 조절할 수 있도록 우하단 리사이즈 핸들과 `window:set-size` IPC를 추가했다.
- 기존 CSS 파츠형 고양이는 fallback으로 남기고, 실제 표시 레이어는 4px 단위 픽셀 box-shadow 스프라이트로 교체했다.
- 품종별로 픽셀 스프라이트의 체형 압축과 꼬리 영역 차이를 적용했다.
- 커스터마이징 플로우와 픽셀 레이어 기준을 `spec.md`에 반영했다.
- 12번 `애니메이션 퀄리티 개선`의 `idle 프레임 추가`를 진행했다.
- idle 상태의 `.cat__sprite`에 오른쪽/왼쪽 방향별 4프레임 `steps(1, end)` 애니메이션을 추가했다.
- 기존 루트 idle 호흡 애니메이션은 유지하고, 스프라이트 내부가 1px 단위로 미세하게 내려앉고 기울어지는 프레임을 더했다.
- `TASKS.md`에서 `idle 프레임 추가`를 완료로 표시했다.
- 다음 단계는 12번 `애니메이션 퀄리티 개선`의 `walk 프레임 추가`이다.
- 12번부터 20번까지 남은 2차 MVP 항목을 한 번에 진행했다.
- walk, sleep, rub, startled, run 상태에 3~4프레임 CSS steps 애니메이션을 추가했다.
- 이동 transition을 `steps` 기반에서 easing 기반으로 바꾸고, 이동마다 `--move-duration`을 다르게 적용해 속도 변화를 추가했다.
- 방향 전환 프레임, 이동 후 idle 행동 진입, 그림자 크기 변화를 정리해 갑작스러운 전환을 줄였다.
- idle 행동 테이블을 추가하고 `sit`, `curl`, `look`, `scratch`, `groom`, `tail` 행동을 확률 기반으로 선택하도록 구현했다.
- idle 행동 쿨다운과 직전 행동 반복 방지 가중치를 추가했다.
- 장시간 입력이 없으면 sleep 행동 가중치가 올라가도록 구현했다.
- 마우스 근처에서는 `data-cat-gaze`로 눈 픽셀 위치가 좌우로 움직이도록 했다.
- 쓰다듬기 방향을 감지해 `data-cat-pet-direction`에 기록하고, rub 반응 시 방향에 맞는 미세 이동과 성공 애니메이션을 적용했다.
- 기존 성격별 이동 빈도, 접근 거리, 놀람 민감도, sleep 빈도, rub 지속시간 설정을 2차 MVP 성격 고도화 항목으로 정리했다.
- 사운드 기본값은 OFF로 두고, purr/발소리/놀람 반응을 Web Audio 기반 짧은 합성음으로 추가했다.
- 설정 UI에 사운드 ON/OFF, 볼륨, 시작 시 실행 옵션을 추가했다.
- 시작 시 실행 옵션은 Electron `setLoginItemSettings` IPC로 연결했다.
- 앱 아이콘 SVG를 추가하고 BrowserWindow, 트레이에 연결했다.
- 마우스 이동 처리를 `requestAnimationFrame`으로 묶어 과도한 이벤트 처리를 줄였다.
- `setCatState` 중복 DOM 업데이트를 방지하고, 간단한 move 샘플링 데이터를 `data-moves-per-minute`로 남기도록 했다.
- 프레임 없는 창 이동/리사이즈, 최소 창 크기, 큰 창/작은 창 경계 보정 흐름을 다시 확인했다.
- `TASKS.md`에서 12번부터 20번까지의 2차 MVP 항목을 완료로 표시했다.
- 실제 1시간 수동 실행, 듀얼 모니터, 고해상도 Windows 실기기 검증은 이 WSL 테스트 환경에서 직접 수행하지 못했으므로 후속 수동 QA 대상으로 남긴다.
- 사용자 피드백을 바탕으로 Desktop Goose류의 오버레이 데스크탑 펫 경험을 3차 MVP 목표로 수립했다.
- 3차 MVP의 핵심 방향을 창 영역감 제거, 평상시 UI 제거, 오버레이 몰입감, 느린 행동 리듬, 마우스를 의식하는 생명체 느낌으로 정리했다.
- `spec.md`에 3차 MVP 목표, 구현 방향, 우선순위, 완료 기준을 추가했다.
- `TASKS.md`에 21번부터 29번까지 3차 MVP 작업 목록을 추가했다.
- 다음 단계는 21번 `Desktop Goose형 오버레이 경험 정리`의 `회색 배경 완전 제거`이다.
- 21번 `Desktop Goose형 오버레이 경험 정리`의 창 영역감 제거를 진행했다.
- 설정 패널의 회색 반투명 배경을 제거해 설정 모드에서도 데스크탑 배경을 덮지 않도록 했다.
- `cat-stage`의 점선 outline과 `--stage-outline` 변수를 제거해 투명 창 경계가 보이지 않게 했다.
- `TASKS.md`에서 21번의 창 영역감 제거 항목을 완료로 표시했다.
- 다음 단계는 21번의 `우하단 리사이즈 핸들 기본 숨김`이다.
- 21번 리사이즈 UI 정리의 일부를 진행했다.
- 우하단 리사이즈 핸들을 companion 상태에서는 숨기고, 설정 모드에서만 사용할 수 있게 했다.
- 평상시 리사이즈 UI가 노출되지 않도록 `TASKS.md`의 관련 항목을 완료로 표시했다.
- 다음 단계는 21번의 `드래그 가능한 영역 최소화`이다.
- 21번 상단 UI 최소화를 진행했다.
- companion 상태에서는 상단 툴바 전체를 숨겨 드래그 가능한 영역과 설정 버튼이 평상시 노출되지 않게 했다.
- 설정 모드에서는 기존 툴바를 유지해 창 이동과 설정 확인이 가능하도록 했다.
- `TASKS.md`에서 드래그 가능한 영역 최소화와 상단 UI 기본 숨김 관련 항목을 완료로 표시했다.
- 다음 단계는 21번의 `트레이 메뉴 기반 설정 구조 검토`이다.
- 트레이 메뉴에 `설정` 항목을 추가했다.
- 트레이의 설정 항목을 누르면 메인 프로세스가 렌더러에 `app:open-settings` 이벤트를 보내고 설정 모드로 전환되게 했다.
- `TASKS.md`에서 21번의 트레이 메뉴 기반 설정 구조 검토를 완료로 표시했다.
- 21번 `Desktop Goose형 오버레이 경험 정리`를 완료했다.
- 다음 단계는 22번 `고양이 존재감 개선`의 `이름표 기본 숨김`이다.
- `TASKS.md` 기준 다음 미완료 항목이 `이름표 기본 숨김`인 것을 확인했다.
- 화면 위 오버레이에서 이름표가 기본 노출되지 않도록 `.cat__name`을 시각적으로 숨김 처리했다.
- 이름 텍스트는 DOM에 유지해 설정값과 접근성 라벨 갱신 흐름은 보존했다.
- `TASKS.md`에서 `이름표 기본 숨김`을 완료로 표시했다.
- 다음 단계는 22번 `고양이 존재감 개선`의 `상태 텍스트 제거`이다.
- `TASKS.md` 기준 다음 미완료 항목이 `상태 텍스트 제거`인 것을 확인했다.
- 현재 UI와 CSS 생성 콘텐츠를 점검한 결과, 고양이 상태값을 화면에 텍스트로 렌더링하는 요소가 없음을 확인했다.
- 상태값은 애니메이션 제어용 `data-*` 속성으로만 유지하고, 디버그 정보 숨김은 다음 별도 항목으로 남겼다.
- `TASKS.md`에서 `상태 텍스트 제거`를 완료로 표시했다.
- 다음 단계는 22번 `고양이 존재감 개선`의 `디버그 정보 숨김`이다.
- `TASKS.md` 기준 다음 미완료 항목이 `디버그 정보 숨김`인 것을 확인했다.
- 앱 루트에 `data-moves-per-minute`를 주기적으로 기록하던 이동 샘플링 디버그 정보를 제거했다.
- 포인터 이벤트 `requestAnimationFrame` 처리와 고양이 애니메이션 제어용 `data-*` 상태값은 동작에 필요하므로 유지했다.
- `TASKS.md`에서 `디버그 정보 숨김`을 완료로 표시했다.
- 다음 단계는 22번 `고양이 존재감 개선`의 `설정 외 UI 최소화`이다.
- `TASKS.md` 기준 다음 미완료 항목이 `설정 외 UI 최소화`인 것을 확인했다.
- 설정 화면 위에 남아 있던 상단 툴바를 작은 창 이동 핸들로 축소했다.
- 설정 화면에서 중복으로 보이던 `설정` 버튼을 숨겨 설정 폼 외부 UI 노출을 줄였다.
- companion 상태에서는 기존처럼 툴바와 리사이즈 핸들이 보이지 않는 흐름을 유지했다.
- `TASKS.md`에서 `설정 외 UI 최소화`를 완료로 표시했다.
- 다음 단계는 22번 `고양이 존재감 개선`의 `화면을 인식하는 느낌 추가`이다.
- 사용자 피드백을 반영해 4차 MVP를 `조용한 공존 기반 데스크탑 개입` 방향으로 정리했다.
- 4차 MVP는 기능 추가보다 존재감 리듬, 방해 강도 제어, 장난 빈도 제한, 최소 소품, 약한 커서 관찰을 우선하도록 `spec.md`와 `TASKS.md`에 추가했다.
- `TASKS.md` 기준 다음 미완료 항목이 `화면을 인식하는 느낌 추가`인 것을 확인했다.
- 고양이 위치가 화면 경계 가까이에 있을 때 `data-cat-screen-edge`를 갱신하는 경계 인식 값을 추가했다.
- 마우스를 보고 있지 않은 상태에서는 경계 가까이에서 눈 픽셀이 화면 안쪽을 보도록 CSS 반응을 추가했다.
- 이번 작업에서는 마우스를 의식하는 시선 처리나 멈칫 행동은 다음 항목으로 남겼다.
- `TASKS.md`에서 `화면을 인식하는 느낌 추가`를 완료로 표시했다.
- 다음 단계는 22번 `고양이 존재감 개선`의 `마우스를 의식하는 시선 처리`이다.
- `TASKS.md` 기준 다음 미완료 항목이 `마우스를 의식하는 시선 처리`인 것을 확인했다.
- 기존 가까운 거리 좌우 시선 처리에서 벗어나, 더 넓은 감지 거리 안에서 마우스 방향을 `left`, `right`, `up`, `down`, `center`로 계산하도록 확장했다.
- 시선 방향별 눈 픽셀 이동 CSS를 추가해 행동 전환 없이도 마우스를 의식하는 느낌이 나도록 했다.
- 이번 작업에서는 천천히 쳐다보기나 멈칫 행동은 다음 항목으로 남겼다.
- `TASKS.md`에서 `마우스를 의식하는 시선 처리`를 완료로 표시했다.
- 다음 단계는 22번 `고양이 존재감 개선`의 `짧은 멈칫 행동 추가`이다.
- `TASKS.md` 기준 다음 미완료 항목이 `짧은 멈칫 행동 추가`인 것을 확인했다.
- 마우스가 가까이 있고 고양이가 idle/sleep 상태일 때 낮은 확률로 짧게 멈칫하는 `data-cat-pausing` 플래그를 추가했다.
- 멈칫 중에는 랜덤 이동이 끼어들지 않도록 이동 조건에 pause 상태를 반영했다.
- CSS에 짧은 몸/스프라이트 멈칫 애니메이션과 눈 픽셀 반응을 추가했다.
- 이번 작업에서는 주변을 살피는 idle과 행동 사이 망설임은 다음 항목으로 남겼다.
- `TASKS.md`에서 `짧은 멈칫 행동 추가`를 완료로 표시했다.
- 다음 단계는 22번 `고양이 존재감 개선`의 `주변을 살피는 idle 추가`이다.
- `TASKS.md` 기준 다음 미완료 항목이 `주변을 살피는 idle 추가`인 것을 확인했다.
- idle 행동 테이블에 `scan` 행동을 추가해 가끔 주변을 살피는 idle이 선택되도록 했다.
- `data-cat-idle-action="scan"` 기준으로 몸과 눈 픽셀이 좌우와 위쪽을 천천히 훑는 CSS 애니메이션을 추가했다.
- 이번 작업에서는 행동 사이의 망설임 로직은 다음 항목으로 남겼다.
- `TASKS.md`에서 `주변을 살피는 idle 추가`를 완료로 표시했다.
- 다음 단계는 22번 `고양이 존재감 개선`의 `행동 사이의 망설임 추가`이다.
- `TASKS.md` 기준 다음 미완료 항목이 `행동 사이의 망설임 추가`인 것을 확인했다.
- 랜덤 이동을 시작하기 전에 짧은 `data-cat-hesitating` 상태를 거치도록 추가했다.
- 망설임 중에는 `scan` idle을 잠깐 보여준 뒤 이동을 시작해 즉시 걷기 시작하는 느낌을 줄였다.
- 마우스 반응, 도망, 쓰다듬기 반응은 이번 작업 범위에서 변경하지 않았다.
- `TASKS.md`에서 `행동 사이의 망설임 추가`를 완료로 표시했다.
- 다음 단계는 22번 `고양이 존재감 개선`의 `사용자에게 과하게 달라붙지 않게 조정`이다.
- `TASKS.md` 기준 다음 미완료 항목이 `사용자에게 과하게 달라붙지 않게 조정`인 것을 확인했다.
- 애교쟁이 접근 반응에 최소 편안한 거리 `COMFORT_DISTANCE`를 추가해 너무 가까운 상태에서는 더 다가가지 않게 했다.
- 애교쟁이 접근 step과 linger 거리를 줄여 사용자 근처에서 랜덤 이동이 과하게 막히는 상황을 완화했다.
- 이번 작업에서는 장시간 근처 체류 방지와 이동 빈도 조정은 다음 항목으로 남겼다.
- `TASKS.md`에서 `사용자에게 과하게 달라붙지 않게 조정`을 완료로 표시했다.
- 다음 단계는 22번 `고양이 존재감 개선`의 `장시간 근처 체류 방지`이다.
- `TASKS.md` 기준 다음 미완료 항목이 `장시간 근처 체류 방지`인 것을 확인했다.
- 애교쟁이가 사용자 근처에 머무르는 linger 상태에 시작 시각을 기록하도록 했다.
- 근처 체류가 `MAX_LINGER_MS`를 넘으면 랜덤 이동 차단을 풀어 자연스럽게 다른 위치로 이동할 수 있게 했다.
- 이번 작업에서는 전체 이동 빈도나 화면 구석 선호 행동은 다음 항목으로 남겼다.
- `TASKS.md`에서 `장시간 근처 체류 방지`를 완료로 표시했다.
- 다음 단계는 22번 `고양이 존재감 개선`의 `너무 자주 이동하지 않게 조정`이다.
- `TASKS.md` 기준 다음 미완료 항목이 `너무 자주 이동하지 않게 조정`인 것을 확인했다.
- 랜덤 이동 판정 간격 `MOVEMENT_CONFIG.INTERVAL_MS`를 4000ms에서 5600ms로 늘려 이동 기회 자체가 너무 자주 오지 않게 했다.
- 이번 작업에서는 이동 확률, idle 비중, 랜덤 딜레이 구조는 후속 항목으로 남겼다.
- `TASKS.md`에서 `너무 자주 이동하지 않게 조정`을 완료로 표시했다.
- 다음 단계는 22번 `고양이 존재감 개선`의 `화면 구석 선호 행동 추가`이다.
- Rosebud 샘플 코드에서 재활용할 만한 요소를 4차 MVP 문서에 반영했다.
- `spec.md`와 `TASKS.md`에 내부 energy/socialTolerance, sleep 기반 energy 회복, 랜덤 blink, 정면/좌/우 에셋 매핑 검토 항목을 추가했다.
- `TASKS.md` 기준 다음 미완료 항목이 `화면 구석 선호 행동 추가`인 것을 확인했다.
- 겁쟁이 전용 구석 이동 로직을 일반 성격도 낮은 확률로 사용할 수 있게 `CORNER_PREFERENCE_CONFIG`를 추가했다.
- 구석 목표는 기존 이동 경계 제한을 그대로 거치며, 겁쟁이는 기존보다 높은 구석 선호 확률을 유지한다.
- `TASKS.md`에서 `화면 구석 선호 행동 추가`를 완료로 표시했다.
- 다음 단계는 23번 `행동 리듬 개선`의 `이동 빈도 감소`이다.
- `TASKS.md` 기준 다음 미완료 항목이 `이동 빈도 감소`인 것을 확인했다.
- 성격별 이동 가중치는 유지하되, 실제 걷기 판정에 전역 `WALK_CHANCE_MULTIPLIER`를 적용해 기본 이동 빈도를 낮췄다.
- 이번 작업에서는 idle 비중 증가, 짧게 움직이고 오래 멈추기, 랜덤 행동 간격 증가는 다음 항목으로 남겼다.
- `TASKS.md`에서 `이동 빈도 감소`를 완료로 표시했다.
- 다음 단계는 23번 `행동 리듬 개선`의 `idle 비중 증가`이다.
- `TASKS.md` 기준 다음 미완료 항목이 `idle 비중 증가`인 것을 확인했다.
- 성격별 기본 이동 성향은 유지하되, 이동 판정 계산에서 `idleWeight`에 전역 `IDLE_WEIGHT_MULTIPLIER`를 적용해 idle 선택 비중을 높였다.
- 사용자 요청에 따라 항상 한국어로 작업하고, 최소 작업 단위로 수정하며, 한국어 커밋/PR 생성 후 자동 머지까지 진행하는 규칙을 `START.md`와 `spec.md`에 보강했다.
- 이번 작업에서는 이동 지속시간, 정지 지속시간, 랜덤 행동 간격은 다음 항목으로 남겼다.
- `TASKS.md`에서 `idle 비중 증가`를 완료로 표시했다.
- 다음 단계는 23번 `행동 리듬 개선`의 `짧게 움직이고 오래 멈추기`이다.
- `TASKS.md` 기준 다음 미완료 항목이 `짧게 움직이고 오래 멈추기`인 것을 확인했다.
- 랜덤 이동 목표가 멀어도 한 번에 이동하는 거리를 `MAX_STEP_DISTANCE` 안으로 제한해 짧게 움직이도록 조정했다.
- 걷기 종료 후 `POST_WALK_REST_MS` 동안 다음 랜덤 이동을 막아 이동 뒤 머무르는 시간을 확보했다.
- 이번 작업에서는 랜덤 행동 간격 증가와 방향 전환 자연화는 다음 항목으로 남겼다.
- `TASKS.md`에서 `짧게 움직이고 오래 멈추기`를 완료로 표시했다.
- 다음 단계는 23번 `행동 리듬 개선`의 `랜덤 행동 간격 증가`이다.
- `TASKS.md` 기준 다음 미완료 항목이 `랜덤 행동 간격 증가`인 것을 확인했다.
- 랜덤 행동 체크 간격 계산에 전역 `ACTION_INTERVAL_MULTIPLIER`를 적용해 성격별 간격 차이는 유지하면서 전체 행동 간격을 늘렸다.
- 이번 작업에서는 방향 전환 자연화와 랜덤 딜레이 추가는 다음 항목으로 남겼다.
- `TASKS.md`에서 `랜덤 행동 간격 증가`를 완료로 표시했다.
- 다음 단계는 23번 `행동 리듬 개선`의 `즉시 반전 이동 제거`이다.
- `TASKS.md` 기준 다음 미완료 항목이 `즉시 반전 이동 제거`인 것을 확인했다.
- 방향이 바뀌는 이동은 먼저 방향 전환 프레임을 보여준 뒤 실제 위치 이동을 시작하도록 공통 이동 헬퍼를 추가했다.
- 애교쟁이 접근, 새침이 회피, 장난꾸러기 추격, 놀람 도망, 랜덤 이동, 장난꾸러기 idle hop에서 즉시 반전 이동이 발생하지 않도록 적용했다.
- 이번 작업에서는 방향 전환 전 추가 pause, 이동 중 감속, run 빈도 조정은 다음 항목으로 남겼다.
- `TASKS.md`에서 `즉시 반전 이동 제거`를 완료로 표시했다.
- 다음 단계는 23번 `행동 리듬 개선`의 `방향 전환 전 pause 추가`이다.
- `즉시 반전 이동 제거` 작업을 `즉시 반전 이동 제거` 커밋으로 기록했다.
- `git push -u origin smooth-turn-before-move`를 시도했으나 `/tmp/gh-bin/gh_2.75.0_linux_amd64/bin/gh`와 VS Code askpass 경로가 없어 GitHub 인증을 진행할 수 없었다.
- 같은 푸시를 권한 상승으로 다시 시도했지만 동일한 인증 오류로 실패했다.
- 따라서 이번 작업의 PR 생성과 자동 머지는 현재 로컬 GitHub 인증 환경 문제로 진행하지 못했다.
- `TASKS.md` 기준 다음 미완료 항목이 `방향 전환 전 pause 추가`인 것을 확인했다.
- 방향이 바뀌는 이동에서 방향 전환 프레임 전에 짧은 `data-cat-turn-pause` 상태를 거치도록 구현했다.
- 랜덤 이동, 성격별 이동 반응, 놀람 도망, 장난꾸러기 idle hop이 공통 pause 시간을 반영하도록 정리했다.
- `START.md`와 `spec.md`에 무조건 한국어로 작업하고, 작업 단위마다 한국어 PR 생성 후 바로 머지까지 진행한다는 규칙을 더 명확하게 기록했다.
- `TASKS.md`에서 `방향 전환 전 pause 추가`를 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 23번 `행동 리듬 개선`의 `이동 중 감속 추가`이다.
- `TASKS.md` 기준 다음 미완료 항목이 `이동 중 감속 추가`인 것을 확인했다.
- 고양이 위치 이동 transition에 `--move-easing` 변수를 추가하고, 기본 이동 easing을 끝으로 갈수록 느려지는 곡선으로 조정했다.
- 이동 좌표 계산, 이동 시간, 상태 전환 타이머는 이번 작업 범위에서 변경하지 않았다.
- `TASKS.md`에서 `이동 중 감속 추가`를 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 23번 `행동 리듬 개선`의 `갑작스러운 run 빈도 감소`이다.
