# STATUS.md

## 2026-05-25

- 현재 저장소 구조를 확인했다.
- 저장소는 초기 상태이며 아직 커밋이 없다.
- 원격 저장소가 설정되어 있지 않은 것을 확인했다.
- 1차 작업 목표에 따라 실제 기능 구현은 시작하지 않고 문서 4개를 생성했다.
- 생성한 문서: `spec.md`, `TASKS.md`, `STATUS.md`, `START.md`
- 원격 저장소 `https://github.com/asteroidin8/i-have-a-cat.git`를 `origin`으로 연결했다.
- 문서 4개 생성 작업을 `초기 문서 작성` 커밋으로 기록했다.
- PR #1 `초기 프로젝트 문서 작성`을 생성했다.
- PR #1을 스쿼시 병합했다.
- 다음 단계는 Electron 기본 구조 생성이다.
- `package.json`을 생성하고 Electron, electron-builder 개발 의존성 선언을 추가했다.
- `TASKS.md`에서 `package.json 생성`을 완료로 표시했다.
- 다음 단계는 Electron 진입 파일 생성이다.
- Prettier, ESLint, Husky를 설치하고 커밋 전 `lint`, `format:check`가 실행되도록 설정했다.
- 개발 도구 설정 파일과 `package-lock.json`을 추가했다.
- Electron 진입 파일 `main.js`를 생성했다.
- `TASKS.md`에서 `Electron 진입 파일 생성`을 완료로 표시했다.
- 다음 단계는 렌더러 HTML/CSS/JS 생성이다.
- 토큰 절약, 파일 분리, 중복 방지, 하드코딩 최소화, 중앙 시스템 관리 원칙을 `START.md`와 `spec.md`에 반영했다.
- 렌더러 기본 파일 `index.html`, `src/renderer.js`, `src/styles/app.css`를 생성했다.
- `TASKS.md`에서 `렌더러 HTML/CSS/JS 생성`을 완료로 표시했다.
- 다음 단계는 개발 실행 스크립트 추가이다.
- `package.json`에 개발 실행 스크립트 `npm start`를 추가했다.
- `TASKS.md`에서 `개발 실행 스크립트 추가`를 완료로 표시했다.
- `timeout 8s npm start`로 개발 실행 스크립트 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 프레임 없는 창 구현이다.
- Electron 창 옵션에 `frame: false`를 추가해 프레임 없는 창을 구현했다.
- `TASKS.md`에서 `프레임 없는 창 구현`을 완료로 표시했다.
- `timeout 8s npm start`로 프레임 없는 창 설정 후 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 투명 배경 구현이다.
- Electron 창 옵션에 `transparent: true`와 투명 `backgroundColor`를 추가했다.
- 렌더러 `html`, `body` 배경을 투명하게 변경했다.
- `TASKS.md`에서 `투명 배경 구현`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 항상 위 표시 구현이다.
- Electron 창 옵션에 `alwaysOnTop: true`를 추가해 항상 위 표시를 구현했다.
- `TASKS.md`에서 `항상 위 표시 구현`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 창 크기 조절 가능 여부 확인이다.
- Electron 창 옵션에 `resizable: true`를 명시해 창 크기 조절 허용 상태를 확인 가능하게 했다.
- `TASKS.md`에서 `창 크기 조절 가능 여부 확인`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 고양이 없는 영역 클릭 통과 구현이다.
- Electron `setIgnoreMouseEvents`와 preload/IPC 브리지를 추가해 고양이 밖 영역에서 마우스 이벤트가 창을 통과하도록 구현했다.
- 렌더러에서 마우스 위치가 고양이 영역 밖이면 클릭 통과를 유지하고, 고양이 영역 위에서는 이후 상호작용을 위해 마우스 이벤트를 다시 받도록 전환했다.
- 렌더러의 `window` 접근을 린트 규칙에서 허용하도록 ESLint 전역 설정을 보강했다.
- `TASKS.md`에서 `고양이 없는 영역 클릭 통과 구현`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 고양이 영역 마우스 이벤트 감지 구현이다.
- 고양이 DOM에 `data-cat-pointer-state`를 추가하고 `mouseenter`, `mousedown`, `mouseup`, `mouseleave` 이벤트로 고양이 영역 마우스 감지를 구현했다.
- 감지 상태는 `outside`, `hover`, `press`로 구분하고, 확인 가능한 최소 시각 피드백을 CSS에 추가했다.
- `TASKS.md`에서 `고양이 영역 마우스 이벤트 감지 구현`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 클릭 통과 관련 제한사항 기록이다.
- `spec.md`에 클릭 통과가 `setIgnoreMouseEvents` 기반이며, 투명 영역에서는 렌더러가 클릭/드래그/휠 입력을 직접 처리할 수 없다는 제한사항을 기록했다.
- Windows 투명 창과 클릭 통과 동작은 운영체제, Electron 버전, 그래픽 환경에 따라 차이가 있어 실제 Windows 환경에서 재검증해야 한다고 기록했다.
- `TASKS.md`에서 `클릭 통과 관련 제한사항 기록`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- 다음 단계는 임시 도트 고양이 표시이다.
- 기존 고양이 DOM 안에 귀, 얼굴, 눈, 코, 꼬리, 발 요소를 추가해 임시 도트 고양이를 표시했다.
- 임시 도트 고양이는 별도 이미지 에셋 없이 HTML/CSS만으로 구성했다.
- `TASKS.md`에서 `임시 도트 고양이 표시`를 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 idle 상태 구현이다.
- 고양이 상태 설정 함수를 추가하고 초기 상태가 `idle`로 설정되도록 정리했다.
- `data-cat-state="idle"` 기준으로 몸 움직임, 눈 깜빡임, 꼬리 움직임을 CSS 애니메이션으로 구현했다.
- `TASKS.md`에서 `idle 상태 구현`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 walk 상태 구현이다.
- 고양이 상태 상수에 `walk`를 추가했다.
- `data-cat-state="walk"` 기준으로 몸, 꼬리, 앞발, 뒷발 움직임을 CSS 애니메이션으로 구현했다.
- 이번 작업에서는 랜덤 이동, 위치 변경, 상태 전환 로직은 추가하지 않았다.
- `TASKS.md`에서 `walk 상태 구현`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 방향 전환 구현이다.
- 고양이 DOM에 `data-cat-direction`을 추가하고 기본 방향을 `right`로 설정했다.
- 고양이 시각 요소를 `cat__sprite`로 감싸 방향 반전이 위치/상태 애니메이션과 충돌하지 않게 했다.
- `data-cat-direction="left"`일 때 스프라이트를 좌우 반전하도록 구현했다.
- `TASKS.md`에서 `방향 전환 구현`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 창 내부 랜덤 이동 구현이다.
- 렌더러에 랜덤 이동 설정과 고양이 위치 설정 함수를 추가했다.
- `cat-stage` 내부 크기를 기준으로 주기적으로 랜덤 좌표를 선택하고 고양이의 `left`, `top`을 갱신하도록 구현했다.
- CSS `transition`으로 위치 이동이 단계적으로 보이게 했다.
- 이번 작업에서는 창 크기 변경 시 경계 보정과 이동/정지 상태 전환은 추가하지 않았다.
- `TASKS.md`에서 `창 내부 랜덤 이동 구현`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 창 경계 밖으로 나가지 않게 제한이다.
- 이동 가능 범위 계산 함수와 좌표 clamp 함수를 추가해 고양이 중심 좌표가 창 내부 범위를 벗어나지 않도록 제한했다.
- 랜덤 이동 대상 좌표와 직접 위치 설정 모두 같은 경계 제한을 거치도록 정리했다.
- 창 크기 변경 시 현재 고양이 위치를 다시 clamp하는 resize 가드를 추가했다.
- `TASKS.md`에서 `창 경계 밖으로 나가지 않게 제한`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 이동/정지 상태 전환 구현이다.
- 랜덤 이동 시작 시 고양이 상태를 `walk`로 전환하고, 위치 이동 transition 시간 이후 `idle`로 되돌리도록 구현했다.
- 이동 상태 타이머를 보관해 다음 이동이 시작되면 이전 타이머를 정리하도록 했다.
- `TASKS.md`에서 `이동/정지 상태 전환 구현`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 마우스 위치 감지이다.
- 렌더러에 `mouseState`를 추가해 최신 마우스 좌표와 창 내부 여부를 저장하도록 했다.
- 기존 `mousemove` 처리에서 마우스 위치를 갱신하고, `mouseleave` 시 창 내부 여부를 false로 전환하도록 구현했다.
- 이번 작업에서는 접근 거리 계산이나 반응 로직은 추가하지 않았다.
- `TASKS.md`에서 `마우스 위치 감지`를 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 접근 거리 계산이다.
- `mouseState`에 고양이 중심까지의 거리 값을 저장하는 `distanceToCat` 필드를 추가했다.
- 고양이 중심 좌표 계산 함수와 두 점 사이 거리 계산 함수를 추가했다.
- 마우스 위치 변경과 고양이 위치 변경 시 접근 거리를 갱신하고, 마우스가 창 밖으로 나가면 거리 값을 비우도록 구현했다.
- 이번 작업에서는 접근 거리 기반 반응 로직은 추가하지 않았다.
- `TASKS.md`에서 `접근 거리 계산`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 쓰다듬기 드래그 판정이다.
- 쓰다듬기 판정을 위한 `pettingState`와 최소 드래그 거리 설정을 추가했다.
- 고양이 위에서 마우스를 누른 뒤 이동한 누적 거리가 기준 이상이면 `data-cat-petting="true"`로 판정하도록 구현했다.
- 드래그 중에는 고양이 영역 밖으로 이동해도 마우스 이벤트 수신이 끊기지 않도록 클릭 통과 전환 조건에 드래그 상태를 반영했다.
- 이번 작업에서는 rub 반응이나 startled/run 반응은 추가하지 않았다.
- `TASKS.md`에서 `쓰다듬기 드래그 판정`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 rub 반응 구현이다.
- 고양이 상태에 `rub`을 추가하고 쓰다듬기 판정 성공 시 `rub` 상태로 전환하도록 구현했다.
- `rub` 반응 중에는 기존 이동 타이머를 정리하고 일정 시간 후 `idle` 상태로 되돌리도록 했다.
- `data-cat-state="rub"`에 몸 흔들림, 꼬리 움직임, 눈 감김 CSS 반응을 추가했다.
- 이번 작업에서는 startled/run 반응은 추가하지 않았다.
- `TASKS.md`에서 `rub 반응 구현`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 startled/run 반응 구현이다.
- 고양이 상태에 `startled`와 `run`을 추가했다.
- 마우스가 고양이 근처에서 빠르게 움직이면 `startled` 상태로 짧게 전환한 뒤 마우스 반대 방향으로 도망가도록 구현했다.
- 도망 위치는 기존 경계 제한 로직을 거치도록 했고, 반응 중에는 랜덤 이동이 끼어들지 않게 했다.
- `data-cat-state="startled"`와 `data-cat-state="run"`에 눈 확대, 점프, 빠른 달리기 CSS 반응을 추가했다.
- `TASKS.md`에서 `startled/run 반응 구현`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 성격 데이터 구조 생성이다.
- 성격 식별자와 기본 성격 값을 추가했다.
- 애교쟁이, 새침이, 겁쟁이, 느긋이, 장난꾸러기 성격 데이터를 이동 성향과 상호작용 성향으로 나누어 정의했다.
- 현재 고양이 프로필의 성격을 조회하고 DOM `data-cat-personality`에 반영하는 함수를 추가했다.
- 이번 작업에서는 성격 데이터를 실제 행동 확률에 적용하지 않았다.
- `TASKS.md`에서 `성격 데이터 구조 생성`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 성격별 행동 확률 적용이다.
- 성격 데이터의 상호작용 확률을 쓰다듬기 `rub` 반응과 빠른 접근 `run` 반응 판정에 적용했다.
- 성격별 `startledSensitivity`로 놀람 반응 속도 기준을 보정하도록 했다.
- 성격별 `rubDurationMultiplier`로 쓰다듬기 반응 지속 시간을 보정하도록 했다.
- 성격별 이동 가중치와 이동 주기 배율을 랜덤 이동 판정에 적용했다.
- 이번 작업에서는 특정 성격별 전용 행동은 추가하지 않았다.
- `TASKS.md`에서 `성격별 행동 확률 적용`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 애교쟁이 구현이다.
- 애교쟁이 성격 전용 접근 반응 설정을 추가했다.
- 애교쟁이 상태에서 마우스가 가까이 있고 빠르게 움직이지 않으면 확률적으로 마우스 방향으로 한 걸음 다가가도록 구현했다.
- 애교쟁이가 사용자 근처에 있을 때는 랜덤 이동이 끼어들지 않게 해 근처에 머무르는 느낌을 추가했다.
- 이번 작업에서는 다른 성격 전용 행동은 추가하지 않았다.
- `TASKS.md`에서 `애교쟁이 구현`을 완료로 표시했다.
- `npx prettier --write src/renderer.js TASKS.md STATUS.md`로 수정 파일 포맷을 정리했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 새침이 구현이다.
- 새침이 성격 전용 회피 반응 설정을 추가했다.
- 새침이 상태에서 마우스가 가까이 있고 빠르게 움직이지 않으면 확률적으로 마우스 반대 방향으로 한 걸음 피하도록 구현했다.
- 새침이 상태에서 쓰다듬기 드래그가 길어지면 rub 반응을 끊고 자리를 피하도록 구현했다.
- 이번 작업에서는 겁쟁이, 느긋이, 장난꾸러기 전용 행동은 추가하지 않았다.
- `TASKS.md`에서 `새침이 구현`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 겁쟁이 구현이다.
- 겁쟁이 성격 전용 설정을 추가했다.
- 겁쟁이 상태에서는 랜덤 이동 시 일정 확률로 창 구석 근처를 목표 위치로 선택하도록 구현했다.
- 겁쟁이 상태에서는 쓰다듬기 판정 시 마우스 이동이 느리면 rub 확률을 높이고 빠르면 rub 확률을 낮추도록 구현했다.
- 이번 작업에서는 느긋이, 장난꾸러기 전용 행동은 추가하지 않았다.
- `TASKS.md`에서 `겁쟁이 구현`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 느긋이 구현이다.
- 느긋이 성격 전용 설정을 추가했다.
- 느긋이 상태에서는 랜덤 이동을 하지 않는 판정에서 성격의 sleep 가중치에 따라 `sleep` 상태로 쉬도록 구현했다.
- 느긋이 상태에서 쓰다듬기 반응은 흔들리는 rub 대신 눈을 감고 가만히 쉬는 `sleep` 상태로 표현하도록 구현했다.
- 이번 작업에서는 장난꾸러기 전용 행동은 추가하지 않았다.
- `TASKS.md`에서 `느긋이 구현`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 장난꾸러기 구현이다.
- 장난꾸러기 성격 전용 설정을 추가했다.
- 장난꾸러기 상태에서 마우스가 근처에서 빠르게 움직이면 도망가지 않고 마우스 방향으로 짧게 뛰어가도록 구현했다.
- 장난꾸러기 상태에서 랜덤 이동을 하지 않는 판정이 나와도 확률적으로 짧게 뛰거나 방향을 바꾸도록 구현했다.
- 이번 작업에서는 커스터마이징 기능은 추가하지 않았다.
- `TASKS.md`에서 `장난꾸러기 구현`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 이름 설정이다.
- 고양이 프로필에 이름 값을 추가하고 기본 이름을 `고양이`로 설정했다.
- 화면 왼쪽 위에 이름 입력 컨트롤을 추가하고 입력값이 고양이 이름 표시와 `aria-label`에 반영되도록 구현했다.
- 이름 입력 컨트롤 영역은 클릭 통과 대상에서 제외해 실제 입력 포커스를 받을 수 있도록 처리했다.
- 이번 작업에서는 이름 저장과 앱 재실행 시 불러오기는 추가하지 않았다.
- `TASKS.md`에서 `이름 설정`을 완료로 표시했다.
- `npm run lint`와 `npm run format:check`를 실행해 통과를 확인했다.
- `timeout 8s npm start`로 앱 실행 호출을 확인했으며, Electron 프로세스는 제한 시간으로 종료했다.
- 다음 단계는 품종 선택이다.
- 품종 식별자와 기본 품종 값을 추가했다.
- 이름 설정 컨트롤 옆에 품종 선택 컨트롤을 추가하고 선택값이 고양이 프로필과 DOM `data-cat-breed`에 반영되도록 구현했다.
- 코리안 숏헤어, 폴드, 짧은 꼬리 품종을 추가하고 품종별 귀와 꼬리 형태 차이를 CSS로 표현했다.
- 이번 작업에서는 품종 저장과 앱 재실행 시 불러오기는 추가하지 않았다.
- `TASKS.md`에서 `품종 선택`을 완료로 표시했다.
- 다음 단계는 눈 색상 선택이다.
- 눈 색상 식별자와 기본 눈 색상 값을 추가했다.
- 품종 선택 컨트롤 옆에 눈 색상 선택 컨트롤을 추가하고 선택값이 고양이 프로필과 DOM `data-cat-eye-color`에 반영되도록 구현했다.
- 초록, 금색, 파랑 눈 색상 프리셋을 추가하고 선택값을 고양이의 `--cat-eye` CSS 변수에 반영하도록 구현했다.
- 이번 작업에서는 눈 색상 저장과 앱 재실행 시 불러오기는 추가하지 않았다.
- `TASKS.md`에서 `눈 색상 선택`을 완료로 표시했다.
- 다음 단계는 털 색상 선택이다.
